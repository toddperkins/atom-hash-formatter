'use babel';

import AtomHashFormatterView from '../lib/atom-hash-formatter-view';

describe('AtomHashFormatterView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
